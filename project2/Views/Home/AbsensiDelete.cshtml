@{
    // View
    View = this;

    // Check action result
    if (absensiDelete.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = absensiDelete.UseLayout ? "_Layout" : "";
}
<script>
var currentTable = @Html.Raw(ConvertToJson(absensiDelete.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { absensi: currentTable } });
var currentPageID = ew.PAGE_ID = "delete";
var currentForm;
var fabsensidelete;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fabsensidelete")
        .setPageId("delete")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@absensiDelete.ShowPageHeader()
@absensiDelete.ShowMessages()
<form name="fabsensidelete" id="fabsensidelete" class="ew-form ew-delete-form" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="delete">
@foreach (string key in absensiDelete.RecordKeys) {
<input type="hidden" name="key_m[]" value="@key">
}
<div class="card ew-card ew-grid @(absensiDelete.TableGridClass)">
<div class="card-body ew-grid-middle-panel @(absensiDelete.TableContainerClass)" style="@(absensiDelete.TableContainerStyle)">
<table class="@(absensiDelete.TableClass)">
    <thead>
    <tr class="ew-table-header">
@if (absensiDelete.id.Visible) { // absensiDelete.id.Visible (begin)
        <th class="@Html.Raw(absensiDelete.id.HeaderCellClass)"><span id="elh_absensi_id" class="absensi_id">@Html.Raw(absensiDelete.id.Caption)</span></th>
} @* absensiDelete.id.Visible (end) *@
@if (absensiDelete.user_id.Visible) { // absensiDelete.user_id.Visible (begin)
        <th class="@Html.Raw(absensiDelete.user_id.HeaderCellClass)"><span id="elh_absensi_user_id" class="absensi_user_id">@Html.Raw(absensiDelete.user_id.Caption)</span></th>
} @* absensiDelete.user_id.Visible (end) *@
@if (absensiDelete.tanggal.Visible) { // absensiDelete.tanggal.Visible (begin)
        <th class="@Html.Raw(absensiDelete.tanggal.HeaderCellClass)"><span id="elh_absensi_tanggal" class="absensi_tanggal">@Html.Raw(absensiDelete.tanggal.Caption)</span></th>
} @* absensiDelete.tanggal.Visible (end) *@
@if (absensiDelete.jam_masuk.Visible) { // absensiDelete.jam_masuk.Visible (begin)
        <th class="@Html.Raw(absensiDelete.jam_masuk.HeaderCellClass)"><span id="elh_absensi_jam_masuk" class="absensi_jam_masuk">@Html.Raw(absensiDelete.jam_masuk.Caption)</span></th>
} @* absensiDelete.jam_masuk.Visible (end) *@
@if (absensiDelete.jam_keluar.Visible) { // absensiDelete.jam_keluar.Visible (begin)
        <th class="@Html.Raw(absensiDelete.jam_keluar.HeaderCellClass)"><span id="elh_absensi_jam_keluar" class="absensi_jam_keluar">@Html.Raw(absensiDelete.jam_keluar.Caption)</span></th>
} @* absensiDelete.jam_keluar.Visible (end) *@
@if (absensiDelete.status.Visible) { // absensiDelete.status.Visible (begin)
        <th class="@Html.Raw(absensiDelete.status.HeaderCellClass)"><span id="elh_absensi_status" class="absensi_status">@Html.Raw(absensiDelete.status.Caption)</span></th>
} @* absensiDelete.status.Visible (end) *@
@if (absensiDelete.lokasi.Visible) { // absensiDelete.lokasi.Visible (begin)
        <th class="@Html.Raw(absensiDelete.lokasi.HeaderCellClass)"><span id="elh_absensi_lokasi" class="absensi_lokasi">@Html.Raw(absensiDelete.lokasi.Caption)</span></th>
} @* absensiDelete.lokasi.Visible (end) *@
@if (absensiDelete.foto_verifikasi.Visible) { // absensiDelete.foto_verifikasi.Visible (begin)
        <th class="@Html.Raw(absensiDelete.foto_verifikasi.HeaderCellClass)"><span id="elh_absensi_foto_verifikasi" class="absensi_foto_verifikasi">@Html.Raw(absensiDelete.foto_verifikasi.Caption)</span></th>
} @* absensiDelete.foto_verifikasi.Visible (end) *@
    </tr>
    </thead>
    <tbody>
@{ absensiDelete.RecordCount = 0; }
@while (absensiDelete.Recordset != null && await absensiDelete.Recordset.ReadAsync()) { // DN
    absensiDelete.RecordCount++;
    absensiDelete.RowCount++;

    // Set row properties
    absensiDelete.ResetAttributes();
    absensiDelete.RowType = RowType.View; // View

    // Get the field contents
    await absensiDelete.LoadRowValues(absensiDelete.Recordset);

    // Render row
    await absensiDelete.RenderRow();
    <tr @Html.Raw(absensiDelete.RowAttributes)>
@if (absensiDelete.id.Visible) { // absensiDelete.id.Visible (begin)
        <td @Html.Raw(absensiDelete.id.CellAttributes)>
<span id="">
<span @Html.Raw(absensiDelete.id.ViewAttributes)>
@Html.Raw(absensiDelete.id.GetViewValue())</span>
</span></td>
} @* absensiDelete.id.Visible (end) *@
@if (absensiDelete.user_id.Visible) { // absensiDelete.user_id.Visible (begin)
        <td @Html.Raw(absensiDelete.user_id.CellAttributes)>
<span id="">
<span @Html.Raw(absensiDelete.user_id.ViewAttributes)>
@Html.Raw(absensiDelete.user_id.GetViewValue())</span>
</span></td>
} @* absensiDelete.user_id.Visible (end) *@
@if (absensiDelete.tanggal.Visible) { // absensiDelete.tanggal.Visible (begin)
        <td @Html.Raw(absensiDelete.tanggal.CellAttributes)>
<span id="">
<span @Html.Raw(absensiDelete.tanggal.ViewAttributes)>
@Html.Raw(absensiDelete.tanggal.GetViewValue())</span>
</span></td>
} @* absensiDelete.tanggal.Visible (end) *@
@if (absensiDelete.jam_masuk.Visible) { // absensiDelete.jam_masuk.Visible (begin)
        <td @Html.Raw(absensiDelete.jam_masuk.CellAttributes)>
<span id="">
<span @Html.Raw(absensiDelete.jam_masuk.ViewAttributes)>
@Html.Raw(absensiDelete.jam_masuk.GetViewValue())</span>
</span></td>
} @* absensiDelete.jam_masuk.Visible (end) *@
@if (absensiDelete.jam_keluar.Visible) { // absensiDelete.jam_keluar.Visible (begin)
        <td @Html.Raw(absensiDelete.jam_keluar.CellAttributes)>
<span id="">
<span @Html.Raw(absensiDelete.jam_keluar.ViewAttributes)>
@Html.Raw(absensiDelete.jam_keluar.GetViewValue())</span>
</span></td>
} @* absensiDelete.jam_keluar.Visible (end) *@
@if (absensiDelete.status.Visible) { // absensiDelete.status.Visible (begin)
        <td @Html.Raw(absensiDelete.status.CellAttributes)>
<span id="">
<span @Html.Raw(absensiDelete.status.ViewAttributes)>
@Html.Raw(absensiDelete.status.GetViewValue())</span>
</span></td>
} @* absensiDelete.status.Visible (end) *@
@if (absensiDelete.lokasi.Visible) { // absensiDelete.lokasi.Visible (begin)
        <td @Html.Raw(absensiDelete.lokasi.CellAttributes)>
<span id="">
<span @Html.Raw(absensiDelete.lokasi.ViewAttributes)>
@Html.Raw(absensiDelete.lokasi.GetViewValue())</span>
</span></td>
} @* absensiDelete.lokasi.Visible (end) *@
@if (absensiDelete.foto_verifikasi.Visible) { // absensiDelete.foto_verifikasi.Visible (begin)
        <td @Html.Raw(absensiDelete.foto_verifikasi.CellAttributes)>
<span id="">
<span @Html.Raw(absensiDelete.foto_verifikasi.ViewAttributes)>
@Html.Raw(absensiDelete.foto_verifikasi.GetViewValue())</span>
</span></td>
} @* absensiDelete.foto_verifikasi.Visible (end) *@
    </tr>
}
@{ absensiDelete.CloseRecordset(); }
</tbody>
</table>
</div>
</div>
<div class="ew-buttons ew-desktop-buttons">
<button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit">@Html.Raw(Language.Phrase("DeleteBtn"))</button>
<button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-href="@AppPath(absensiDelete.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
</div>
</form>
@absensiDelete.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
